[tool.ruff]
line-length = 100
target-version = "py311"

[tool.ruff.lint]
select = ["E", "F", "I"]  # erreurs pycodestyle, pyflakes, tri des imports
ignore = ["E501"]  # la longueur de ligne est gérée par line-length, pas bloquante
